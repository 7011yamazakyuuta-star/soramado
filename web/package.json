{
  "name": "soramado-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Turn your display into a window to an endless sky. Physically-based atmospheric scattering renderer (WebGL2).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "icons": "node scripts/gen-icons.mjs"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vite": "^5.4.11",
    "vite-plugin-pwa": "^0.20.5"
  }
}
